POSTGRES_PASSWORD=replace-with-a-long-random-value
ADMIN_TOKEN=replace-with-at-least-32-random-characters
DATABASE_URL=postgresql://livedash:replace-with-a-long-random-value@localhost:5432/livedash
HOST=127.0.0.1
PORT=3000
NOTIFICATIONS_ENABLED=false
# APNS_TEAM_ID=
# APNS_KEY_ID=
# APNS_TOPIC=
# APNS_KEY_FILE=/absolute/path/to/AuthKey.p8

# Direct (non-Compose) runtime: private directory, owner-only permissions
# BLOB_ROOT=/absolute/private/live-dashboard-blobs
